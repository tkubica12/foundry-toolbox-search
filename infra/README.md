# infra

`provision.py` creates the resource group, Foundry resource/project/model deployment, Container Apps MCP services, project connections, and toolbox.

Run with `uv run --project infra python -m infra.provision --prefix <unique-prefix>`.
