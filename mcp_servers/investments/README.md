# investments MCP

Mock portfolio, suitability, and market-insight tools for the Foundry toolbox demo.

Local run: `uv run --project mcp_servers/investments uvicorn investments_mcp.app:app --port 8002`.
